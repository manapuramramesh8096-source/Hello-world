#include <stdio.h>

int main() {
  
    Int a,b,c;
    printf("Enter three numbers: ");
    
        scanf("%d %d %d",&a,&b,c);

    if (a >= b) {
        if (a >= c) {GZ
            printf("%d is the largest number.\n", a);
        } else {
            printf("%d is the largest number.\n", c);
        }
    } else {
        if (b >= c) {
            printf("%d is the largest number.\n", a);
        } else {
            printf("%d is the largest number.\n", b);
        }
    }

    return 0;
}
